#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @a: a - variable
 * @b: b - variable
 * Return: always 0.
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
